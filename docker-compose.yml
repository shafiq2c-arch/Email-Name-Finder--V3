version: "3.9"

services:
  api:
    build: .
    ports:
      - "8010:8000"
    environment:
      - SEARXNG_BASE_URL=https://searxngapp.app.digitalgalaxy.com:8080
      - GROQ_API_KEY=${GROQ_API_KEY}
      - OPENROUTER_API_KEY=${OPENROUTER_API_KEY}